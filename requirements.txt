# Core
openai>=1.30
python-dotenv>=1.0
matplotlib>=3.7
nbformat>=5.9

# Real-world AI-engineering stack -- REQUIRED. The workshop uses these live, so
# create the accounts and put the keys in your .env BEFORE the session (see SETUP.md).
langfuse>=4.0          # LLM observability / tracing (v4 SDK; instruments every llm() call)
wandb>=0.18            # experiment tracking (NB2 learning curve, NB4 SkillOpt "training")
qdrant-client>=1.12    # managed vector DB for skill / memory retrieval (NB3, NB5)
langgraph>=1.0         # agent orchestration framework (later refactor notebook)
